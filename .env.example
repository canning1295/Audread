## Example environment variables (copy to .env and fill in locally)
# Never commit your filled .env file. Prefer setting production secrets in the Netlify UI.

# Vite client env (must be prefixed with VITE_)
VITE_DATA_PROVIDER=netlify-db

# Netlify / Providers (server-side only)
PONS_API_KEY=
OPENAI_API_KEY=
LIBRETRANSLATE_URL=

# Netlify Blobs (optional local dev overrides)
NETLIFY_BLOBS_SITE_ID=
