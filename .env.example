## Environment variables for Netlify deployment
## These should be configured in Netlify Dashboard: Site configuration → Environment variables
## NOT used for local .env file - this project deploys via Netlify only

# NOTE: Authentication is no longer required. All data is stored locally in IndexedDB.
# The following variables are only needed for server-side API calls (TTS, dictionary, etc.)

# Netlify / Providers (server-side only)
PONS_API_KEY=
OPENAI_API_KEY=
LIBRETRANSLATE_URL=

# Netlify Blobs (optional - only if using Netlify Functions for cloud features)
NETLIFY_BLOBS_SITE_ID=
